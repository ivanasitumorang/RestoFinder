<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.azuka.restofinder.favorite">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_feature_favorite">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>

        <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <activity android:name=".feature.favoritelist.FavoriteListActivity" />
        <activity android:name=".feature.detail.DetailActivity" />
    </application>

</manifest>